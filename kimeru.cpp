string parse_Heading(String cur,int index)
{
  int h_count=0;
  while(index<cur.size())
  {
    if (cur[i]=='#')h_count++;
    else break; 
    i++;
  }
  string lev=h+n_count;
  return <lev><<cur.substr(index,cur.size())<< </lev>;
}
string parse_Links(String cur,int index)
{
  int open=0;
  bool link=false;
  string content;
  string link;
  return null;
}
